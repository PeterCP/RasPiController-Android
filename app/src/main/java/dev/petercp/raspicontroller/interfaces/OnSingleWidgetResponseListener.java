package dev.petercp.raspicontroller.interfaces;

import dev.petercp.raspicontroller.classes.BaseWidget;

public interface OnSingleWidgetResponseListener {
    void onSingleWidgetResponse(BaseWidget widget);
}
